/**
 * Created by tjsticke on 6/12/2015.
 */
//var app = angular.module('productManagement',[]);

(function () {
    // define the main module
    "use strict";   // strict mode   javascript will catch some exceptions
    // must use var etc..
    //var app = angular.module("productManagement", []);
    var app = angular.module("productManagement",
                            ["common.services"]);   //now we define our dependency of common.services
}());



